{
  "name": "webable-backend",
  "version": "1.0.0",
  "description": "WebAble Pro backend — Stripe billing + Gemini proxy for paying users",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node lib/db.js",
    "stripe:setup": "node scripts/setup-stripe.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^17.4.0"
  },
  "devDependencies": {}
}
